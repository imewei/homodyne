# Sphinx documentation dependencies for ReadTheDocs
# This file is used by ReadTheDocs build process
# The main package is installed via pip install -e ".[docs]"

# Core Sphinx
sphinx>=8.0.0,<9.0.0

# Theme
sphinx-rtd-theme>=3.0.0,<4.0.0

# Markdown support
myst-parser>=4.0.0,<5.0.0

# Optional enhancements for better documentation
sphinx-autodoc-typehints>=2.0.0,<3.0.0
sphinx-copybutton>=0.5.2,<0.6.0

# Note: Core dependencies (jax, numpy, etc.) are installed via pip install -e ".[docs]"
# This ensures version consistency with pyproject.toml
